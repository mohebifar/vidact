# Changelog

Changesets generates the release history beside each public package. Add a
Markdown file under `.changeset/` for each pull request that changes published
behavior; the coordinated Version Packages pull request consumes those files
and updates package versions and changelogs.
