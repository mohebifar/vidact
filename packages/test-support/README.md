# @vidact/test-support

Test-only helpers for compiled Vidact applications. `act` drains Vidact-owned
work deterministically, while the MutationObserver helpers assert that an
update stays inside its declared DOM mutation envelope. This package adds no
production runtime code.
